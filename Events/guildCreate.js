const client = require('../index')
const { pushSlashCommand } = require('../controllers/utils')

client.on('guildCreate', async function (guild) {

  // Insert the guild id into the tblDiscordGuild table
 
})
